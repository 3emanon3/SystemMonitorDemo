A small demo of desktop application to monitor real-time CPU and Memory usage.
Implemented MVVM architecture for clean separation of logic and UI using WPF (XAML).
Utilized C# multi-threading (Task/Async) to update UI without freezing the main thread.
Integrated a C++ DLL using P/Invoke to fetch low-level system uptime data.